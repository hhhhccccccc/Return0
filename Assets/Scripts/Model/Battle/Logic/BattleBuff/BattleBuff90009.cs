using cfg;

//todo 反击buff三息内不会低于1层
public class BattleBuff90009 : BattleBuffBase
{
/*if (Subject != null)
{
    var buffID = Config.ParamList[0].ToInt();
    var buff = Subject.GetBuff(buffID);
    if (buff != null)
    {
        var isAdd = Config.ParamList[1].ToInt() == 1;
        var layerCount = Config.ParamList[2].ToInt();
        buff.AddBuffNotLowerLayerCount(isAdd, layerCount);
    }
}*/
}
